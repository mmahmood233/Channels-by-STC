// Dashboard layout — sidebar + topbar + main content area
// Will be implemented in the page-by-page phase
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
